apiVersion: apps/v1
kind: StatefulSet                         #Definimos que es un StatefulSet
metadata:
  name: mongo
spec:
    serviceName: mongo                    #
    replicas: 3
    selector:
        matchLabels:
          app: mongo
    template:
        metadata:
          labels:
              app: mongo
        spec:
          containers:
          - name: mongo
            image: mongo
            ports:
            - containerPort: 27017
              name: mongo                #